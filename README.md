# order-flow-service
OrderFlow is a backend Order &amp; Returns Management System designed for managing the complete lifecycle of e-commerce orders. The system implements state machines for order and return workflows, supports asynchronous background job processing for invoice generation and refund handling, and ensures reliable tracking of all state transitions for audit.
